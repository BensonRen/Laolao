# Laolao - macOS dependencies
# Install: pip install -r requirements-macos.txt
#
# NOTE: mlx-whisper requires Apple Silicon (M1/M2/M3/M4).
#       If you are on an Intel Mac, use requirements.txt instead:
#         pip install -r requirements.txt

faster-whisper>=1.0.0
sounddevice>=0.4.6
numpy>=1.24.0
websockets>=12.0

# Apple Silicon Neural Engine acceleration (M1/M2/M3/M4 only)
mlx-whisper>=0.4.0

# Silero-VAD (same as requirements.txt)
silero-vad>=5.0

# Traditional → Simplified Chinese conversion
opencc-python-reimplemented>=0.1.7

# Virtual camera output (Electron app only — not needed for plain CLI/OBS mode)
pyvirtualcam>=0.10.0
Pillow>=10.0.0
